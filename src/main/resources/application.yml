spring:
  profiles:
    # 개발 : dev  실서비스 : prd  테스트 : test
    active: prd

---

spring:
  config:
    activate:
      on-profile: dev

---

spring:
  config:
    activate:
      on-profile: prod

---

spring:
  config:
    activate:
      on-profile: test
